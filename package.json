{
  "name": "blog",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.11.0"
  },
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate": "npm run sync:projects && npm run generate:note-meta && npm run generate:search-index && npm run check:source && nuxt generate && node scripts/prepare-cloudflare-pages.mjs && npm run check:dist",
    "generate:note-meta": "node scripts/generate-note-meta.mjs",
    "generate:search-index": "node scripts/generate-search-index.mjs",
    "sync:projects": "node scripts/sync-github-projects.mjs",
    "check:content": "npm run generate:note-meta && npm run generate:search-index && npm run check:source",
    "check:source": "node scripts/check-content.mjs --source",
    "check:dist": "node scripts/check-content.mjs --dist",
    "new": "node scripts/new-content.mjs",
    "prepare": "nuxt prepare",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "nuxt typecheck",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@nuxt/content": "^3.15.0",
    "@nuxtjs/robots": "^6.1.2",
    "@nuxtjs/sitemap": "^8.2.2",
    "better-sqlite3": "^12.11.1",
    "katex": "^0.17.0",
    "mermaid": "^11.16.0",
    "nuxt": "^4.4.8",
    "rehype-katex": "^7.0.1",
    "remark-math": "^6.0.0"
  },
  "devDependencies": {
    "@nuxt/eslint": "^1.16.0",
    "@types/node": "^26.1.1",
    "@vue/test-utils": "^2.4.11",
    "eslint": "^10.7.0",
    "prettier": "^3.9.5",
    "vitest": "^4.1.10",
    "vue-tsc": "^3.3.7"
  }
}
